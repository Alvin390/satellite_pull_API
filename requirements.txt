# === Core Scientific Libraries ===
numpy~=1.26.4           # Numerical operations
pandas~=2.1.4+dfsg      # DataFrames, time series, tabular data
rasterio~=1.3.9         # Geospatial raster data (TIFFs)
SciPy~=1.11.4           # Scientific computing (ndimage.label, etc.)

# === Satellite & Geospatial APIs ===
sentinelhub>=3.8        # Sentinel Hub API (required for fetch_satellite.py)
affine~=2.4.0           # Rasterio dependency for affine transforms

# === Climate & Weather Data ===
openmeteo-requests>=1.2 # Open-Meteo API client
requests~=2.31.0        # HTTP requests (Open-Meteo, CHIRPS, etc.)
requests-cache>=1.1     # Caching for requests (used in fetch_climate.py)
retry-requests>=2.0     # Retry logic for requests (used in fetch_climate.py)

# === File Formats & Parsing ===
tomli~=2.0.1            # TOML config parsing (Python <3.11)
PyYAML~=6.0.1           # (Optional, for YAML if needed)
python-dateutil~=2.8.2  # Date/time parsing (pandas dependency)

# === Logging & Utilities ===
colorama~=0.4.6         # Colored terminal output (optional, for logging)
rich~=13.7.1            # Rich logging/tracebacks (optional)

# === Testing ===
pytest~=7.4.4           # Unit testing

# === Data Visualization (Optional) ===
matplotlib~=3.6.3       # Plotting (if used for diagnostics)

# === Other Dependencies (from environment or indirect) ===
lxml~=5.2.1
beautifulsoup4~=4.12.3
soupsieve~=2.5
packaging~=24.0
typing_extensions~=4.10.0
html5lib~=1.1
pip~=24.0
attrs~=23.2.0
distro~=1.9.0
wheel~=0.42.0
cryptography~=41.0.7
docutils~=0.20.1
Sphinx~=7.2.6
tornado~=6.4
Jinja2~=3.1.2
filelock~=3.13.1
h5py~=3.10.0
tables~=3.9.2
pytz~=2024.1
fsspec~=2024.2.0
urllib3~=2.0.7
dask~=2023.12.1+dfsg
idna~=3.6
pyparsing~=3.1.1
click~=8.1.6
setuptools~=68.1.2
netCDF4~=1.6.5
mpmath~=0.0.0
sympy~=1.12
msgpack~=1.0.3
fusepy~=3.0.1
lz4~=4.0.2+dfsg
pygit2~=1.14.1
distributed~=0+unknown
paramiko~=2.12.0
libarchive-c~=2.9
MarkupSafe~=2.1.5
xlrd~=2.0.1
openpyxl~=3.1.2
odfpy~=1.4.2
numexpr~=2.9.0
py~=1.11.0
certifi~=2023.11.17
xarray~=2024.2.0
zarr~=2.17.0
cftime~=1.6.3
Bottleneck~=1.3.5
h5netcdf~=1.3.0
six~=1.16.0
oauthlib~=3.2.2
blinker~=1.7.0
cligj~=0.7.2
click-plugins~=1.1.1
chardet~=5.2.0
numcodecs~=0.12.1+ds
more-itertools~=10.2.0
zipp~=1.0.0